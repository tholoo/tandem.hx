// Start the controller in the background and export TANDEM_SOCKET first.
// "hx" must be the Steel-enabled Helix build with tandem.scm loaded.
layout {
    pane split_direction="vertical" {
        pane command="hx" size="70%"
        pane command="tandem" size="30%" {
            args "chat"
        }
    }
}
